## About

This repo contains the source code to my personal blog including the github action to publish it to github pages. 

## Local build

run `npm install` to initialize the  project. Then run  `npm run dev` or `npm run build`

